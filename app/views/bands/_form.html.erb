<form action="<%= bands_url %>" method="POST">
  <input  type="hidden"
          name="autheticity_token"
          value="<%= form_authenticity_token %>">
  <p>Band name: <input type=text name=band[name]></p>
  <input type=submit value "Kick up the jams">
</form>